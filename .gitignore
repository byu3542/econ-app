# Local configuration — contains API keys, never commit
local.properties

# Built outputs — contain secrets baked in via BuildConfig
build/
app/build/
.gradle/
*.apk
*.aab

# Android Studio
.idea/
*.iml
.DS_Store

# Keystore files
*.jks
*.keystore
