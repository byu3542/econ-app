<?xml version="1.0" encoding="utf-8"?>
<!-- Compact suggestion card — used horizontally in the AI chat sheet.
     Tapping anywhere sends the prompt; the old button is kept for
     adapter compatibility but hidden. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="250dp"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="@dimen/space_md"
    android:layout_marginEnd="@dimen/space_sm"
    android:background="@drawable/bg_chip_outline"
    android:clickable="true"
    android:focusable="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="top">

        <TextView
            android:id="@+id/tvPromptEmoji"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🏦"
            android:textSize="@dimen/text_body"
            android:layout_marginEnd="@dimen/space_sm" />

        <TextView
            android:id="@+id/tvPromptText"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="How might this Fed action affect Treasury yields?"
            android:textSize="@dimen/text_secondary"
            android:textColor="@color/text_navy"
            android:maxLines="2"
            android:ellipsize="end"
            android:lineSpacingMultiplier="1.15" />
    </LinearLayout>

    <Button
        android:id="@+id/btnUsePrompt"
        android:layout_width="match_parent"
        android:layout_height="36dp"
        android:layout_marginTop="@dimen/space_sm"
        android:text="Use This Prompt"
        android:textSize="@dimen/text_secondary"
        android:visibility="gone" />
</LinearLayout>
